---
name: user profile
description: Setup del usuario y preferencias de colaboración.
type: user
---

- Trabaja en Windows 11; usa Claude Code vía CLI dentro de **WSL Ubuntu**.
- Bun instalado en `~/.bun/bin/bun` dentro de WSL (no en Windows nativo).
- Repo está en `/mnt/c/Users/kilso/Downloads/DeppRRHH/` (filesystem de Windows montado en WSL — más lento que `~/`, considerar mover si hay quejas de perf).
- Prefiere respuestas directas y concisas en **español**.
- Le interesa entender opciones antes de implementar (le gusta que se le pregunte por DB/auth/deploy en lugar de asumir).
- No tiene problema con stack moderno agresivo (TS strict, monorepo, drizzle, etc.).
